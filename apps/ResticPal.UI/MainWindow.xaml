<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ResticPal.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="resticpal">

    <NavigationView
        IsBackButtonVisible="Collapsed"
        IsSettingsVisible="True"
        Loaded="NavigationView_Loaded"
        PaneDisplayMode="Left"
        PaneTitle="resticpal"
        SelectedItem="{x:Bind OverviewItem}">
        <NavigationView.MenuItems>
            <NavigationViewItem x:Name="OverviewItem" Content="Overview" Tag="overview">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Home" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Backup sources" Tag="sources">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Folder" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Repository" Tag="repository">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Save" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Schedule" Tag="schedule">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Calendar" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="History" Tag="history">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Clock" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <ScrollViewer>
            <Grid Padding="32" RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Spacing="6">
                    <TextBlock x:Name="StatusTitle" Style="{StaticResource TitleTextBlockStyle}" Text="Connecting" />
                    <TextBlock x:Name="StatusDescription"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="Waiting for the resticpal service"
                        TextWrapping="Wrap" />
                </StackPanel>

                <Border
                    Grid.Row="1"
                    Padding="24"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <Grid ColumnSpacing="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <FontIcon
                            FontSize="32"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Glyph="&#xE753;" />
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock x:Name="StatusCardTitle" FontSize="18" FontWeight="SemiBold" Text="Service connection pending" />
                            <TextBlock x:Name="StatusCardDescription"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="The first implementation slice is ready for named-pipe status wiring."
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                </Border>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
                    <Button x:Name="RunBackupButton" Click="RunBackupButton_Click" Content="Run backup now" IsEnabled="False" />
                    <Button x:Name="CancelBackupButton" Click="CancelBackupButton_Click" Content="Cancel backup" IsEnabled="False" />
                    <Button Content="Open settings" IsEnabled="False" />
                </StackPanel>

                <InfoBar
                    x:Name="MessageBar"
                    Grid.Row="3"
                    IsClosable="True"
                    IsOpen="False" />
            </Grid>
        </ScrollViewer>
    </NavigationView>
</Window>
